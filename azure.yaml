# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: hivecraft-backend
metadata:
  template: hivecraft-backend

infra:
  path: azure/infra

services:
  web:
    project: .
    language: ts
    host: containerapp
